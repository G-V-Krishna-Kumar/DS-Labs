#include <stdio.h>
#include <stdlib.h>


struct Node
{
    int data;
    struct Node* next;
};

struct Node* last = NULL;

void InsertAtEnd()
{
    struct Node* node = (struct Node*)malloc(sizeof(struct Node));
    
}


int main()
{
    return 0;
}